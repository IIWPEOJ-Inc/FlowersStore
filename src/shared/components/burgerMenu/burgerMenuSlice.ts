import { createSlice } from '@reduxjs/toolkit';

const initialState = {};

const burgerMenuSlice = createSlice({
  name: 'burgerMenu',
  initialState,
  reducers: {},
});

// export const {} = burgerMenuSlice.actions;

export default burgerMenuSlice.reducer;
